@model IEnumerable<Indicator>

<div class="container">
    <div class="table-wrapper">
        <div class="table-title">
            <div class="row">
                <form asp-controller="Indicator" asp-action="Index" method="post">
                    <p>
                        Показание: <input type="text" name="search" />
                        <input type="submit" value="Поиск" />
                    </p>
                </form>
                <div class="col-sm-6">
                    <a asp-controller="Indicator" asp-action="Create" class="btn btn-success">
                        <i class="material-icons">&#xE147;</i>
                        <span>Добавить показания</span>
                    </a>
                </div>
            </div>
        </div>
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Id)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.TargetId)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.SetupDate)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Rate)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Value)
                    </th>
                    <th>Действия</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(model => item.Id)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.TargetId)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.SetupDate)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.Rate)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.Value)
                        </td>
                        <td>
                            <a asp-action="Update" asp-controller="Indicator" asp-route-id="@item.Id" class="edit">Редактирование</a>
                            <a asp-action="Delete" asp-controller="Indicator" asp-route-id="@item.Id" class="delete">Удаление</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>