@model IEnumerable<Organization>

<div class="container">
    <div class="table-wrapper">
        <div class="table-title">
            <div class="row">
                <form asp-controller="Organization" asp-action="Index" method="post">
                    <p>
                        Название: <input type="text" name="search" />
                        <input type="submit" value="Поиск" />
                    </p>
                </form>
                <div class="col-sm-6">
                    <a asp-controller="Organization" asp-action="Create" class="btn btn-success">
                        <i class="material-icons">&#xE147;</i>
                        <span>Зарегистрировать организацию</span>
                    </a>
                </div>
            </div>
        </div>
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Id)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Name)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Ownership)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.DirectorName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.DirectorPhone)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.SecretaryName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.SecretaryPhone)
                    </th>
                    <th>Действия</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(model => item.Id)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.Ownership)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.DirectorName)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.DirectorPhone)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.SecretaryName)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.SecretaryPhone)
                        </td>
                        <td>
                            <a asp-action="Update" asp-controller="Organization" asp-route-id="@item.Id" class="edit">Редактирование</a>
                            <a asp-action="Delete" asp-controller="Organization" asp-route-id="@item.Id" class="delete">Удаление</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>