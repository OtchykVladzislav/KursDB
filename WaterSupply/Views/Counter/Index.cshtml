@model IEnumerable<CounterVM>

<div class="container">
    <div class="table-wrapper">
        <div class="table-title">
            <div class="row">
                <form asp-controller="Counter" asp-action="Index" method="post">
                    <p>
                        Марка: <input type="text" name="search" />
                        <input type="submit" value="Поиск" />
                    </p>
                </form>
                <div class="col-sm-6">
                    <a asp-controller="Counter" asp-action="Create" class="btn btn-success">
                        <i class="material-icons">&#xE147;</i>
                        <span>Добавить счётчик</span>
                    </a>
                </div>
            </div>
        </div>
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Id)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Mark)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Organization)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.SetupDate)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.SetupPlace)
                    </th>
                    <th>Действия</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(model => item.Id)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.Mark)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.Organization)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.SetupDate)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.SetupPlace)
                        </td>
                        <td>
                            <a asp-action="Update" asp-controller="Counter" asp-route-id="@item.Id" class="edit">Редактирование</a>
                            <a asp-action="Delete" asp-controller="Counter" asp-route-id="@item.Id" class="delete">Удаление</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>