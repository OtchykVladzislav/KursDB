@model IEnumerable<IndicatorCheck>

<div class="container">
    <div class="table-wrapper">
        <div class="table-title">
            <div class="row">
                <form asp-controller="Check" asp-action="Index" method="post">
                    <p>
                        Дата: <input type="text" name="search" />
                        <input type="submit" value="Поиск" />
                    </p>
                </form>
                <div class="col-sm-6">
                    <a asp-controller="Check" asp-action="Create" class="btn btn-success">
                        <i class="material-icons">&#xE147;</i>
                        <span>Добавить проверку</span>
                    </a>
                </div>
            </div>
        </div>
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Id)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.IndicatorId)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Date)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Info)
                    </th>
                    <th>Действия</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(model => item.Id)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.IndicatorId)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.Date)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.Info)
                        </td>
                        <td>
                            <a asp-action="Update" asp-controller="Check" asp-route-id="@item.Id" class="edit">Редактирование</a>
                            <a asp-action="Delete" asp-controller="Check" asp-route-id="@item.Id" class="delete">Удаление</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>